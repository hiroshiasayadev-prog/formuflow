---
scope: docs/spec/06-formula/06b-expression.md
status: confirmed
last_updated: 2026-04-12
summary: >
  式エリア・出力型自動推論・KaTeXプレビュー・カスタムフォーマットの仕様。
  変数名の色分けルールとリアルタイムバリデーションを定義する。
key_decisions:
  - 変数名色分け: 定義済み→青ハイライト、未定義→白字+赤波線
  - 出力型はデフォルト自動推論（手動切り替え可）
  - KaTeXはオプトイン（デフォルトOFF）
  - カスタムフォーマット内の変数名は内部IDで追跡し自動置換
  - 式が空/エラー時は出力型をNone表示（自動推論ONのまま）
depends_on:
  - docs/spec/06-formula/index.md
  - docs/spec/06-formula/06a-inputs.md   # 変数名定義を参照してバリデーション
---

# 06b — 式エリア・出力型・KaTeX仕様

---

## 式エリア（FORMULA）

- 1行テキスト入力（式が長くなる場合は折り返し表示）
- リアルタイムバリデーション
- 変数名の色分け:
  - 引数（INPUTS）に定義済みの変数名 → 青ハイライト
  - 未定義の変数名 → 白字 + 赤波線
- エラーメッセージ: 式エリアの直下に表示
- 確定タイミング: フォーカスアウト / Enter

---

## 出力型

- デフォルト: 「自動推論」チェックON → 推論結果を表示（編集不可）
- チェックOFF → プルダウンで手動選択。選択肢はPhase 5で確定（TBD）
- 推論できない場合（式が空・エラー等）は `None` を表示

---

## KaTeXプレビュー

- 式エリアの変更に連動してリアルタイム更新
- 引数の変数名をそのまま使ったデフォルト表示
- カスタムフォーマットが有効な場合はそちらを優先

---

## KaTeXフォーマット（オプション）

- 「カスタムフォーマットを設定する」チェックボックス。デフォルトOFF
- チェックONで入力欄を表示（式エリアと同サイズ）
- LaTeX記法で記述。例: `ivChar + \frac{vBias}{gain}`
- 変数名の色分け（式エリアと同じルール）:
  - 引数に定義済みの変数名 → 青ハイライト
  - 未定義の変数名 → 白字 + 赤波線
- 引数の変数名が変更された場合: 内部IDで追跡し自動置換
- KaTeXプレビューにリアルタイム反映

---

## State Diagrams

### D-06-5: 出力型の自動推論状態

```mermaid
stateDiagram-v2
    auto : 自動推論ON（デフォルト）<br/>推論結果表示・編集不可
    manual : 手動選択<br/>（プルダウン有効）
    none : None表示<br/>（自動推論ON・式が空/エラー）

    auto --> manual : 自動推論チェックOFF
    manual --> auto : 自動推論チェックON
    auto --> none : 式が空またはエラー
    none --> auto : 式が正常に評価できる状態に戻る
```

### D-06-6: KaTeXフォーマットのON/OFF

```mermaid
stateDiagram-v2
    off : OFF（デフォルト）<br/>カスタム入力欄非表示
    on : ON<br/>カスタム入力欄表示

    off --> on : 「カスタムフォーマットを設定する」チェックON
    on --> off : チェックOFF
```
